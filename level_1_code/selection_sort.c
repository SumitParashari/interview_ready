// For ascending order slection sort:
// Find the minimum element in unsorted array and swap it with element at the begining.


#include <stdio.h>

void selection_sort(int **array, int size)
{
     for(i; i< size; i++)
     {
	     for(j; j<)
     }
}

int main()
{
	int i,data[10];
        int size = sizeof(data)/sizeof(int);
	
	for(i=0; i < size; i++)
	{
	    scanf("%d", &data[i]);
	}
	 
	for(i=0; i < size; i++)
        {
            printf("%d ", data[i]);
        }
	printf("\n");
}
